# 开发问题记录

## Tailwind CSS 高度 calc 不生效

**问题：** `h-[calc(100%-56)]` 不生效

**原因：**

1. `56` 缺少单位，应为 `56px`
2. CSS `calc()` 中 `-` 运算符两侧必须有空格，`calc(100%-56px)` 无效
3. Tailwind 任意值语法中空格用下划线 `_` 代替

**解决：** `h-[calc(100%_-_56px)]`

**更优方案：** 用 flex 布局，`main` 设 `flex-1` 自动撑满剩余高度，无需手动计算。
